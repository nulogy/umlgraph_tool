config:
  TdcRoot:
    - '@hidden'
  UMLOptions:
    - '@opt vertical'
    - '@opt attributes'
    - '@opt operations'
    - '@hidden'

application gold:
  ApplicationSingularGenerator extends SingularGenerator:
  ApplicationStandardGenerator extends StandardGenerator:

roles lightcoral:
  GeneratorBase:
    - '@has - - - CatalogEntries'
    - '@has - - - DataDefinition'
  ConfigurableGenerator extends GeneratorBase:
    - '@depend - - - DefinitionSourcable'
  SingularGenerator extends ConfigurableGenerator:
  StandardGenerator extends ConfigurableGenerator:

core palegreen:
  CatalogEntries:

factories lightblue:
  DefinitionResolvable:
  DefinitionSourcable:
  InstanceDefinitionConfigurable extends DefinitionSourcable:
    - '@hidden'

tools gray:
  DataDefinition:
  InMemoryDataDefinition extends DataDefinition:
  DataDefinitionFileReader extends DataDefinition:
