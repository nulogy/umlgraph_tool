config:
  QCloud:
    - '@hidden'
  UMLOptions:
    - '@opt vertical'
    - '@opt attributes'
    - '@opt operations'
    - '@hidden'

base white:
  Company:
    - '@composed - - - Sheet'
    - '@hidden'

  Site:
    - '@hidden'

  User:
    - '@hidden'

base lightgray:
  Category:
    - '@composed - - - CategoryHeader'
    - '@composed - - - Form'

  CategoryHeader:
    - '@composed - - - FormHeader'

base palegreen:
  Form:
    - '@composed - - - FormHeader'
    - '@composed - - - Check'
    - '@composed - - - Sheet'

  FormHeader:
    - '@composed - - - SheetHeaderValue'

  Check:
    - '@stereotype XML'

base lightblue:
  Sheet:
    - '@composed - - - SheetHeaderValue'
    - '@composed - - - Inspection'
    - '@composed - - - SheetActivityDetail'

  SheetHeaderValue:

  Inspection:
    - '@composed - - - CheckValue'

  CheckValue:
    - '@stereotype XML'

  SheetActivityDetail:

